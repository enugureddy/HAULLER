<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600&family=Open+Sans&display=swap" rel="stylesheet">

  <style>
    :root {
      --porter-blue: #0052CC;
      --hover-blue: #003399;
      --light-grey: #F5F7FA;
      --charcoal: #2E2E2E;
      --alert-red: #dc3545;
    }

    body {
      margin: 0;
      padding: 0;
      background: var(--light-grey);
      font-family: 'Open Sans', sans-serif;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    header {
      width: 100%;
      background-color: var(--porter-blue);
      color: white;
      padding: 1rem 2rem;
      font-size: 1.5rem;
      font-family: 'Montserrat', sans-serif;
      font-weight: 700;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .back-btn {
      background-color: var(--alert-red);
      border: none;
      padding: 8px 16px;
      font-weight: bold;
      color: white;
      border-radius: 6px;
      transition: 0.3s ease;
    }

    .back-btn:hover {
      background-color: #c82333;
    }

    .container-box {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 2rem;
    }

    .formdes {
      background: white;
      padding: 2.5rem;
      width: 100%;
      max-width: 600px;
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);
    }

    h3 {
      text-align: center;
      font-family: 'Montserrat', sans-serif;
      font-weight: 600;
      margin-bottom: 2rem;
      color: var(--charcoal);
    }

    label {
      font-weight: 600;
      color: var(--charcoal);
      margin-bottom: 0.5rem;
      display: block;
    }

    .form-control {
      background-color: var(--light-grey);
      border: 1px solid #ccc;
      color: #000;
      padding: 12px;
      font-size: 1rem;
      border-radius: 8px;
    }

    .form-control:focus {
      border-color: var(--porter-blue);
      box-shadow: 0 0 0 0.2rem rgba(0, 82, 204, 0.25);
      background-color: #fff;
    }

    textarea.form-control {
      min-height: 150px;
      resize: vertical;
    }

    .btn-primary {
      background-color: var(--porter-blue);
      border: none;
      padding: 12px;
      font-weight: bold;
      font-size: 1rem;
      border-radius: 8px;
      width: 100%;
      transition: background 0.3s;
    }

    .btn-primary:hover {
      background-color: var(--hover-blue);
    }

    footer {
      text-align: center;
      padding: 1rem;
      background-color: white;
      color: #666;
      font-size: 0.9rem;
      border-top: 1px solid #ddd;
    }
  </style>
</head>

<body>

  <!-- Header -->
  <header>
    Request Information
    <button class="back-btn" onclick="history.back()">← Back</button>
  </header>

  <!-- Form Container -->
  <div class="container-box">
    <form action="/admin/requestpost" method="POST" class="formdes">
      <h3>Submit Your Request</h3>

      <div class="mb-3">
        <label>Ad ID</label>
        <input type="text" class="form-control" name="id" value="<%= mdata._id %>" readonly required>
      </div>

      <div class="mb-3">
        <label>Your Email</label>
        <input type="text" class="form-control" name="email" value="<%= mdata.email %>" readonly required>
      </div>

      <div class="mb-3">
        <label>Request Details</label>
        <textarea class="form-control" name="description" placeholder="Please describe your request in detail..." required></textarea>
      </div>

      <button type="submit" class="btn btn-primary">Submit Request</button>
    </form>
  </div>

  <!-- Footer -->
  <footer>
    &copy; <%= new Date().getFullYear() %> Hauller Admin | Built by Team 40 – SIT725
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
