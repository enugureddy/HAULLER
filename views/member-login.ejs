<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Member Login</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@900&family=Open+Sans:wght@700&display=swap" rel="stylesheet">

    <style>
        body {
            background: url('https://www.tiptopequipment.com.au/skin/frontend/tiptop/default/images/page-elements/fleet-4.jpg') no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            font-family: 'Open Sans', sans-serif;
            margin: 0;
            padding: 0;
        }

        .header-container {
            text-align: center;
            margin-bottom: 2rem;
            animation: pulse 2s infinite alternate;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            100% { transform: scale(1.03); }
        }

        .brand-name {
            font-family: 'Montserrat', sans-serif;
            font-size: 8rem;
            font-weight: 900;
            letter-spacing: 4px;
            margin-bottom: 0;
            text-transform: uppercase;
            color: #fff;
            text-shadow: 4px 4px 0 #ff6600, 
                        8px 8px 0 rgba(0,0,0,0.2);
            position: relative;
            display: block;
            line-height: 1;
        }

        .brand-name::after {
            content: '';
            position: absolute;
            bottom: -15px;
            left: 50%;
            transform: translateX(-50%);
            width: 200px;
            height: 8px;
            background: linear-gradient(to right, #ff9900, #ff6600);
            border-radius: 8px;
        }

        .tagline {
            font-size: 2.5rem;
            font-weight: 800;
            letter-spacing: 2px;
            margin-top: 2rem;
            color: white;
            background-color: rgba(78, 190, 228, 0.8);
            padding: 15px 30px;
            border-radius: 10px;
            display: inline-block;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            text-shadow: 2px 2px 2px rgba(0,0,0,0.3);
        }

        .formdes {
            padding: 3rem;
            width: 50%; 
            max-width: 600px;
            min-width: 400px;
            border-radius: 20px;
            box-shadow: 0 0 1rem 0 rgba(0, 0, 0, .2);
            background-color: rgba(255, 255, 255, 0.945);
            backdrop-filter: blur(5px);
            margin-top: 2rem;
        }

        .form-control {
            background-color: rgba(255, 255, 255, 0.25);
            border: none;
            color: rgb(0, 0, 0);
            padding: 12px;
            font-size: 1.1rem;
            height: calc(1.5em + 1rem + 2px);
        }

        .form-control::placeholder {
            color: rgba(0, 0, 0, 0.8);
            font-size: 1.1rem;
        }

        .form-control:focus {
            background-color: rgba(12, 0, 0, 0.35);
            color: rgb(0, 0, 0);
            box-shadow: none;
        }

        .btn-custom {
            background-color: #007bff;
            border: none;
            width: 100%;
            padding: 12px;
            font-weight: bold;
            font-size: 1.1rem;
            border-radius: 8px;
            color: rgb(0, 0, 0);
            margin-top: 10px;
        }

        .btn-custom:hover {
            background-color: #0056b3;
            color: white;
        }

        .link-btn {
            width: 100%;
            margin-top: 15px;
            padding: 12px;
            background-color: transparent;
            border: 2px solid white;
            color: rgb(0, 0, 0);
            border-radius: 8px;
            text-decoration: none;
            display: inline-block;
            text-align: center;
            transition: all 0.3s ease;
            font-size: 1.1rem;
        }

        .link-btn:hover {
            background-color: rgb(235, 118, 118);
            color: black;
            text-decoration: none;
        }

        h3 {
            text-align: center;
            font-weight: bold;
            margin-bottom: 30px;
            color: rgb(0, 0, 0);
            font-size: 2rem;
        }

        .form-label {
            color: rgb(0, 0, 0);
            font-size: 1.5rem;
            margin-bottom: 12px;
        }
    </style>
</head>
<body>

    <div class="header-container">
        <h1 class="brand-name">HAULLER</h1>
        <p class="tagline">UTE HAULING SERVICES</p>
    </div>

    <form action="/member/loginverify" method="post" class="formdes">
        <h3>Member Login</h3>

        <div class="mb-4">
            <label for="email" class="form-label">Member Email</label>
            <input type="email" class="form-control" id="email" name="email" placeholder="Enter Email" required>
        </div>

        <div class="mb-4">
            <label for="password" class="form-label">Member Password</label>
            <input type="password" class="form-control" id="password" name="password" placeholder="Enter Password" required>
        </div>

        <button type="submit" class="btn btn-custom">Login</button>

        <a href="/member/member-register" class="link-btn">New User? Register Here</a>
        <a href="/member/member-forgotpassword" class="link-btn">Forgot Password?</a>
    </form>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>