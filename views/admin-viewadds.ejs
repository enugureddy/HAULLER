<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Posted Ads – Admin | Hauller</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Fonts + Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&family=Open+Sans&display=swap" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet" />

  <style>
    :root {
      --porter-blue: #0052CC;
      --hover-blue: #003399;
      --light-grey: #F5F7FA;
      --charcoal: #2E2E2E;
      --alert-red: #EF4444;
    }

    body {
      background-color: var(--light-grey);
      font-family: 'Open Sans', sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    /* Header */
    .main-header {
      background-color: var(--porter-blue);
      color: white;
      padding: 1.25rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
    }

    .main-header h2 {
      margin: 0;
      font-family: 'Montserrat', sans-serif;
      font-weight: 700;
    }

    .btn-back {
      background-color: var(--alert-red);
      color: white;
      padding: 10px 20px;
      font-weight: 600;
      border: none;
      border-radius: 6px;
      transition: background 0.3s ease;
    }

    .btn-back:hover {
      background-color: #c62828;
    }

    /* Main Content */
    .container-main {
      padding: 2rem;
      flex-grow: 1;
    }

    .glass-card {
      background: white;
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.08);
    }

    .page-title {
      font-family: 'Montserrat', sans-serif;
      font-size: 1.8rem;
      font-weight: 700;
      margin-bottom: 1.5rem;
      color: var(--charcoal);
      text-align: center;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    thead {
      background-color: var(--porter-blue);
      color: white;
    }

    th, td {
      padding: 14px 18px;
      text-align: center;
      border-bottom: 1px solid #e5e5e5;
      vertical-align: middle;
    }

    tbody tr:hover {
      background-color: #f1f5ff;
    }

    a.view-btn {
      background-color: #e7f1ff;
      padding: 6px 14px;
      border-radius: 5px;
      font-weight: 600;
      color: var(--porter-blue);
      text-decoration: none;
      transition: 0.3s;
      display: inline-block;
    }

    a.view-btn:hover {
      background-color: var(--porter-blue);
      color: white;
    }

    footer {
      text-align: center;
      padding: 1rem;
      font-size: 0.9rem;
      color: #666;
      background: white;
      border-top: 1px solid #ddd;
    }

    @media (max-width: 768px) {
      .main-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
      }

      th, td {
        font-size: 0.9rem;
      }

      .page-title {
        font-size: 1.5rem;
      }
    }
  </style>
</head>

<body>

  <!-- HEADER -->
  <header class="main-header">
    <h2>Hauller Admin Panel</h2>
    <button class="btn-back" onclick="window.location.href='back'"><i class="fas fa-arrow-left me-2"></i>Back</button>
  </header>

  <!-- MAIN CONTENT -->
  <main class="container-main">
    <div class="glass-card">
      <div class="page-title">Ads Posted by Members</div>

      <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Created On</th>
            <th>Price ($)</th>
            <th>Image</th>
            <th>Request Info</th>
          </tr>
        </thead>
        <tbody>
          <% if(addData.length > 0) {
            addData.forEach(function(data) { 
              var dObj = new Date(data.timestamp);
              var date = dObj.getDate()+"/"+(dObj.getMonth()+1)+"/"+dObj.getFullYear()+" "+ dObj.getHours()+":"+dObj.getMinutes()+":"+dObj.getSeconds();
          %>
              <tr>
                <td><%= data.name %></td>
                <td><%= data.description %></td>
                <td><%= date %></td>
                <td><%= data.price %></td>
                <td><a class="view-btn" href="/admin/view/<%= data._id %>">View</a></td>
                <td><a class="view-btn" href="/admin/request/<%= data._id %>">Request</a></td>
              </tr>
          <% }) } else { %>
              <tr><td colspan="6">No ads available.</td></tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>

  <!-- FOOTER -->
  <footer>
    &copy; <%= new Date().getFullYear() %> Hauller Admin Panel. Built by Team 40 | SIT725
  </footer>

  <!-- Bootstrap -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
