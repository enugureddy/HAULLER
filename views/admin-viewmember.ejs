<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Admin Dashboard – Members | Hauller</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&family=Open+Sans&display=swap" rel="stylesheet" />
  <!-- Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">

  <style>
    :root {
      --porter-blue: #0052CC;
      --light-grey: #F5F7FA;
      --charcoal: #2E2E2E;
      --hover-blue: #003399;
      --vibrant-cyan: #00C2FF;
    }

    body {
      background: var(--light-grey);
      font-family: 'Open Sans', sans-serif;
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* HEADER */
    .main-header {
      background-color: var(--porter-blue);
      color: white;
      padding: 1.25rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    .main-header h2 {
      font-family: 'Montserrat', sans-serif;
      font-weight: 700;
      margin: 0;
    }

    .btn-logout {
      background-color: #EF4444;
      border: none;
      padding: 10px 20px;
      border-radius: 6px;
      color: white;
      font-weight: bold;
      transition: 0.3s;
    }

    .btn-logout:hover {
      background-color: #cc0000;
    }

    /* MAIN CONTENT */
    .container-main {
      flex-grow: 1;
      padding: 2rem;
    }

    .glass-card {
      background: white;
      border-radius: 16px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.08);
      padding: 2rem;
    }

    .section-title {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--charcoal);
      margin-bottom: 1.5rem;
      font-family: 'Montserrat', sans-serif;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    thead {
      background-color: var(--porter-blue);
      color: white;
    }

    th, td {
      padding: 12px 18px;
      text-align: center;
      border-bottom: 1px solid #ddd;
    }

    tbody tr:hover {
      background-color: #f1f5ff;
    }

    a.view-btn {
      color: var(--porter-blue);
      font-weight: 600;
      text-decoration: none;
      padding: 6px 14px;
      border-radius: 5px;
      background-color: #e7f1ff;
      transition: 0.2s ease-in-out;
      display: inline-block;
    }

    a.view-btn:hover {
      background-color: var(--porter-blue);
      color: white;
    }

    /* FOOTER */
    footer {
      text-align: center;
      padding: 1rem;
      background: #fff;
      font-size: 0.9rem;
      border-top: 1px solid #eee;
      color: #444;
    }

    @media (max-width: 768px) {
      .section-title {
        font-size: 1.5rem;
      }

      .main-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
      }
    }
  </style>
</head>

<body>

  <!-- HEADER -->
  <header class="main-header">
    <h2>Hauller Admin Panel</h2>
    <button class="btn btn-logout" onclick="window.location.href='logout'"><i class="fas fa-sign-out-alt me-2"></i>Logout</button>
  </header>

  <!-- MAIN -->
  <main class="container-main">
    <div class="glass-card">
      <div class="section-title">Registered Members</div>

      <table class="table">
        <thead>
          <tr>
            <th>User Name</th>
            <th>User Email</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% if (data.length > 0) { 
            data.forEach(function (user) { %>
              <tr>
                <td><%= user.name %></td>
                <td><%= user.email %></td>
                <td><a class="view-btn" href="/admin/viewmemberadds/<%= user._id %>"><i class="fas fa-eye"></i> View Ads</a></td>
              </tr>
          <% }) } else { %>
              <tr><td colspan="3">No members found.</td></tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>

  <!-- FOOTER -->
  <footer>
    &copy; <%= new Date().getFullYear() %> Hauller. Admin Panel – Team 40, SIT725.
  </footer>

  <!-- Scripts -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
