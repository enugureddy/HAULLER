<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Guest Login - Hauller</title>

  <!-- Bootstrap CSS & Fonts -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@900&family=Open+Sans:wght@600&display=swap" rel="stylesheet">

  <style>
    :root {
      --porter-blue: #0052CC;
      --hover-blue: #003399;
      --vibrant-cyan: #00C2FF;
      --light-grey: #F5F7FA;
      --charcoal: #2E2E2E;
    }

    body {
      margin: 0;
      padding: 0;
      background: url('https://www.tiptopequipment.com.au/skin/frontend/tiptop/default/images/page-elements/fleet-4.jpg') no-repeat center center fixed;
      background-size: cover;
      height: 100vh;
      font-family: 'Open Sans', sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    .header-container {
      text-align: center;
      margin-bottom: 2rem;
      animation: pulse 2s infinite alternate;
    }

    @keyframes pulse {
      0% { transform: scale(1); }
      100% { transform: scale(1.03); }
    }

    .brand-name {
      font-family: 'Montserrat', sans-serif;
      font-size: 5rem;
      font-weight: 900;
      letter-spacing: 4px;
      color: white;
      text-shadow: 4px 4px 0 var(--vibrant-cyan), 6px 6px 10px rgba(0, 0, 0, 0.25);
      position: relative;
    }

    .brand-name::after {
      content: '';
      position: absolute;
      bottom: -12px;
      left: 50%;
      transform: translateX(-50%);
      width: 180px;
      height: 6px;
      background: linear-gradient(to right, var(--vibrant-cyan), var(--porter-blue));
      border-radius: 8px;
    }

    .tagline {
      font-size: 1.8rem;
      font-weight: 700;
      background-color: rgba(0, 82, 204, 0.8);
      color: white;
      padding: 12px 24px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
      margin-top: 1rem;
      display: inline-block;
    }

    .formdes {
      background: rgba(255, 255, 255, 0.95);
      padding: 2.5rem;
      max-width: 500px;
      width: 90%;
      border-radius: 16px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
      backdrop-filter: blur(8px);

      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
    }

    h3 {
      font-family: 'Montserrat', sans-serif;
      font-weight: 700;
      color: var(--charcoal);
      margin-bottom: 1rem;
    }

    .subtext {
      font-size: 1rem;
      font-weight: 500;
      color: #333;
      margin-bottom: 2rem;
    }

    .btn-primary {
      background-color: var(--porter-blue);
      border: none;
      width: 100%;
      padding: 12px;
      font-size: 1.1rem;
      font-weight: bold;
      border-radius: 8px;
      transition: background 0.3s ease;
    }

    .btn-primary:hover {
      background-color: var(--hover-blue);
    }

    footer {
      margin-top: 2rem;
      font-size: 0.9rem;
      color: #eee;
    }

    @media (max-width: 600px) {
      .brand-name {
        font-size: 3rem;
      }
      .tagline {
        font-size: 1.1rem;
        padding: 10px 18px;
      }
      .formdes {
        padding: 1.8rem;
      }
    }
  </style>
</head>

<body>
  <div class="header-container">
    <h1 class="brand-name">HAULLER</h1>
    <p class="tagline">UTE HAULING SERVICES</p>
  </div>

  <form action="/guest/enter" method="post" class="formdes">
    <h3>Guest Login</h3>
    <p class="subtext">Click below to explore as a guest.</p>
    <button type="submit" class="btn btn-primary">Enter as Guest</button>
  </form>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
