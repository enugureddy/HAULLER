<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Member Register</title>

  <!-- Bootstrap + Fonts + Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600&family=Open+Sans&display=swap" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet" />

  <style>
    :root {
      --porter-blue: #0052CC;
      --light-grey: #F5F7FA;
      --charcoal: #2E2E2E;
    }

    html, body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
      background: #F5F7FA;
      background-size: cover;
      font-family: 'Open Sans', sans-serif;
      display: flex;
      flex-direction: column;
    }

    .main-header {
      background-color: var(--porter-blue);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      color: white;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    .main-header h2 {
      margin: 0;
      font-family: 'Montserrat', sans-serif;
      font-weight: 700;
    }

    .user-actions {
      display: flex;
      gap: 1rem;
    }

    .user-actions button {
      background: white;
      border: none;
      border-radius: 50%;
      width: 40px;
      height: 40px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .user-actions button i {
      color: var(--porter-blue);
      font-size: 1rem;
    }

    main {
      flex-grow: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .formdes {
      padding: 2.5rem;
      width: 100%;
      max-width: 600px;
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(31, 38, 135, 0.37);
      background-color: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.18);
    }

    h3 {
      text-align: center;
      font-family: 'Montserrat', sans-serif;
      font-weight: 600;
      color: #000;
      margin-bottom: 2rem;
      font-size: 2rem;
      text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
    }

    label {
      color: #000;
      font-weight: 500;
      margin-bottom: 0.5rem;
      text-shadow: 0 1px 2px rgba(0,0,0,0.3);
    }

    .form-control {
      background-color: rgba(255, 255, 255, 0.25);
      border: none;
      color: #000;
      padding: 12px;
      margin-bottom: 1.5rem;
    }

    .form-control::placeholder {
      color: rgba(0, 0, 0, 0.7);
    }

    .form-control:focus {
      background-color: rgba(255, 255, 255, 0.35);
      border: 1px solid rgba(255, 255, 255, 0.5);
      outline: none;
    }

    .password-info {
      color: rgba(32, 24, 24, 0.7);
      font-size: 0.9rem;
      margin-top: -1rem;
      margin-bottom: 1rem;
    }

    footer {
      text-align: center;
      padding: 1rem;
      background: #fff;
      font-size: 0.9rem;
      border-top: 1px solid #eee;
      color: #444;
    }

    footer a {
      color: var(--porter-blue);
      text-decoration: none;
    }

    footer a:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>

  <!-- Header -->
  <header class="main-header">
    <div><h2>Hauller</h2></div>
  </header>

  <!-- Form -->
  <main>
    <form action="/member/registerpost" method="post" class="formdes" enctype="multipart/form-data">
      <h3>Member Registration</h3>

      <div class="mb-3">
        <label>Member Name</label>
        <input type="text" class="form-control" id="name" name="name" placeholder="Enter your full name" required>
      </div>

      <div class="mb-3">
        <label>Member Email</label>
        <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email address" required>
      </div>

      <div class="mb-3">
        <label>Member Password</label>
        <input type="password" class="form-control" id="password" name="password"
               placeholder="Create password (5-10 characters)"
               minlength="5" maxlength="10" required>
      </div>

      <div class="mb-3">
        <label>Profile Image (optional)</label>
        <input type="file" class="form-control" name="pimage" id="pimage" accept="image/*" />
      </div>

      <button type="submit" class="btn btn-primary">Register</button>
    </form>
  </main>

  <!-- Footer -->
  <footer>
    &copy; <%= new Date().getFullYear() %> Hauller. No rights reserved. For demonstration purposes only. | SIT 725 – Team 40
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
