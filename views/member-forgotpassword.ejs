<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Member Forgot Password</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500;700&family=Open+Sans&display=swap" rel="stylesheet">
    
    <style>
        body {
            background: lightblue center center fixed;
            background-size: cover;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Open Sans', sans-serif;
            margin: 0;
            padding: 20px;
        }

        .formdes {
            padding: 2.5rem;
            width: 40%;
            min-width: 400px;
            max-width: 600px;
            border-radius: 20px;
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
            background-color: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.18);
        }

        h3 {
            text-align: center;
            font-family: 'Montserrat', sans-serif;
            font-weight: 500;
            color: rgb(0, 0, 0);
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
            margin-bottom: 2rem;
            font-size: 1.5rem;
            line-height: 1.4;
        }

        .form-control {
            background-color: whitesmoke;
            border: none;
            color: rgb(0, 0, 0);
            padding: 12px;
            margin-bottom: 1.5rem;
        }

        .form-control::placeholder {
            color: rgba(0, 0, 0, 0.7);
        }

        .form-control:focus {
            background-color: rgba(255, 255, 255, 0.35);
            color: rgb(0, 0, 0);
            box-shadow: none;
            border: 1px solid rgba(255, 255, 255, 0.5);
        }

        label {
            color: rgb(0, 0, 0) !important;
            font-weight: 500;
            margin-bottom: 0.5rem;
            display: block;
            text-shadow: 0 1px 2px rgba(0,0,0,0.3);
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
            padding: 12px;
            font-weight: bold;
            font-size: 1.1rem;
            border-radius: 8px;
            margin-top: 1rem;
            transition: all 0.3s ease;
            width: 100%;
        }

        .btn-primary:hover {
            background-color: #0069d9;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
        }

        .info-text {
            color: rgba(31, 23, 23, 0.8);
            font-size: 0.9rem;
            text-align: center;
            margin-top: 1rem;
        }
    </style>
</head>

<body>
    <form action="/member/sendpassword" method="post" class="formdes">
        <h3>Forgot Password? No worries! Enter your registered email and we'll send you a password reset link</h3>
        
        <div class="mb-3">
            <label>Member Email</label>
            <input type="email" class="form-control" id="email" name="email" placeholder="Enter your registered email" required>
        </div>
        
        <button type="submit" class="btn btn-primary">Send Reset Link</button>
        
        <p class="info-text">Check your spam folder if you don't see our email</p>
    </form>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>