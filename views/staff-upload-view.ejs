






<html>
    <head>
        <head>
            <style>
.formdes {
    padding: 2%;
    width: 35%;
    margin-top: 4%;
    margin-left: 30%;
    border-radius: 20px;
    box-shadow: 0 0 1rem 0 rgba(0, 0, 0, .2); 
    background-color: rgba(255, 255, 255, .15);
    backdrop-filter: blur(5px);
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.formdes .form-control,
.formdes button {
    width: 100%;
}

    
                body{
                background: url('https://cdn.pixabay.com/photo/2021/10/08/11/11/shopping-6691014_1280.png');
                background-repeat: no-repeat;
                background-size: cover;
                }
                 .alert {
            margin-bottom: 15px;
            color: red;
            font-weight: bold;
        }
            </style>
    
            <!-- CSS only -->
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
            <!-- JavaScript Bundle with Popper -->
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
            
        <title> <%= title %> </title>
        
    </head>

    <body>
        <center>
            
        </center>
        <form action="/member/uploadaction" method="post" enctype="multipart/form-data" class="formdes">
            <h3 style="color: rgb(0, 0, 0);"><center>Add Addvertisement</center></h3>
            <div class="mb-3">
                <label> Name </label>
                <input type="text" class="form-control" name="name" required />
            </div>
            <div class="mb-3">
                <label> Description </label>
                <input type="text" class="form-control" name="description" required />
            </div>
            <div class="mb-3">
                <label>Price </label>
                <input type="number" class="form-control" name="price" required />
            </div>
            
            <div class="mb-3">
                <label> File </label>
                <input type="file" class="form-control" id="adimage" name="adimage" required />
                <div id="clientError" class="alert" style="display:none;"></div>
            </div>
            <div>
                <button type="submit" class="btn btn-primary w-100" id="submitBtn"> Add advert </button>
            </div>
        </form>

        <script>
            const fileInput = document.getElementById('adimage');
            const errorDiv = document.getElementById('clientError');
            const submitBtn = document.getElementById('submitBtn');
    
            fileInput.addEventListener('change', function() {
                const file = this.files[0];
                if (file) {
                    const fileName = file.name.toLowerCase();
                    const validExtension = fileName.endsWith('.jpg');
    
                    if (!validExtension) {
                        errorDiv.textContent = "Only .jpg files are allowed.";
                        errorDiv.style.display = 'block';
                        submitBtn.disabled = true;
                    } else {
                        errorDiv.style.display = 'none';
                        submitBtn.disabled = false;
                    }
                }
            });
        </script>
    </body>

</html>