<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%=data.name%> - Ad View</title>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600&family=Open+Sans&display=swap" rel="stylesheet">
    
    <style>
        body {
            background: lightblue no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Open Sans', sans-serif;
            margin: 0;
            padding: 20px;
        }

        .formdes {
            padding: 2.5rem;
            width: 40%;
            min-width: 400px;
            max-width: 600px;
            border-radius: 20px;
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
            background-color: whitesmoke;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.18);
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            font-family: 'Montserrat', sans-serif;
            font-weight: 600;
            color: rgb(0, 0, 0);
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
            margin-bottom: 1rem;
            font-size: 2rem;
            text-align: center;
        }

        h2 {
            font-family: 'Open Sans', sans-serif;
            font-weight: 400;
            color: rgba(0, 0, 0, 0.9);
            text-shadow: 0 1px 2px rgba(0,0,0,0.2);
            margin-bottom: 2rem;
            font-size: 1.3rem;
            text-align: center;
            max-width: 80%;
        }

        .image-container {
            width: 100%;
            max-width: 400px;
            height: 300px;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            border-radius: 10px;
            background-color: rgba(255, 255, 255, 0.1);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            margin: 0 auto 2rem;
        }

        .image-container img {
            max-width: 100%;
            max-height: 100%;
            width: auto;
            height: auto;
            object-fit: contain;
            object-position: center;
        }
    </style>
</head>

<body>
    <form class="formdes">
        <h1><%=data.name%></h1>
        <h2><%=data.description%></h2>
        
        <div class="image-container">
            <img src="<%=data.image%>" alt="<%=data.name%> advertisement image">
        </div>
    </form>
</body>
</html>